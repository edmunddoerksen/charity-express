/**
* @jest-environment jsdom
*/

import '@testing-library/jest-dom/extend-expect';

// render tests

test('Tags dropdown header ', () => {
  // render(<SearchResultsDisplay />);
  // const tagsText = screen.getByTestId('tagstest');
  // expect(tagsText).toBeInTheDocument();
});

test('Company statement renders', () => {
  // render(<SearchResultsDisplay />);
  // const compState = screen.getByTestId('companyStatement');
  // expect(compState).toBeInTheDocument();
});
